# Templates

Starter skeletons so new work doesn't start from a blank file:

- Notebook template (standard imports, header cells, boilerplate)
- Script template for a new `utils_lya_halo` module
- Spec template — copy this into `specs/` when starting a new analysis

Drop your existing templates here, or ask Claude to generate starting versions once your real notebooks/modules are in place.
